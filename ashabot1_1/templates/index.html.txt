<!DOCTYPE html>
<html>
  <head>
    <title>My Page</title>
  </head>
  <body>
    <h1>Welcome to My Page</h1>
    //<form id="query-form">
    
    {% if answer %}
    <p>{{ answer }}</p>
    {% endif %}
    
    
    <form method="post">
      {% csrf_token %}
      <label for="query">What is your query?</label>
      <input type="text" id="query" name="query">
      <button type="submit">Submit</button>
    </form>
    <p>Your answer is: <span id="answer"></span></p>
    <script>
      function submitQuery(event) {
        event.preventDefault(); // prevent the form from submitting normally

        // get the query from the input field
        const query = document.getElementById("query").value;

        // create a JSON object with the query
        const data = {
          query: query
        };

        // send a POST request to the API
        fetch("/api/query", {
          method: "POST",
          headers: {
            "Content-Type": "application/json"
          },
          body: JSON.stringify(data)
        })
        .then(response => response.json())
        .then(data => {
          // get the answer from the response and display it on the page
          const answer = data.answer;
          document.getElementById("answer").textContent = answer;
        })
        .catch(error => console.error(error));
      }

      // add an event listener to the form to
